#!/usr/bin/env bash
if [ -z "$1" ]; then
	echo "Usage: $0 <Application Name>"
	exit 1
fi

APP_NAME="$1"

BUNDLE_ID=$(osascript -e "id of application \"$APP_NAME\"" 2>/dev/null)

if [ -z "$BUNDLE_ID" ]; then
	echo "Application '$APP_NAME' not found or not installed."
	exit 1
fi

echo $BUNDLE_ID
